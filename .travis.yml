sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - master # build master branch only
script:
  - hexo generate # generate static files
deploy:
  type: github 
  repository: https://github.com/ioslide/ioslide.github.io    //改成自己的用户名
  branch: master    
  # type: github      //改成github
  # repository: https://github.com/jinyanhuan/jinyanhuan.github.io    //改成自己的用户名
  # branch: master
  # provider: pages
  # skip-cleanup: true
  # github-token: $GH_TOKEN
  # keep-history: true
  # on:
  #   branch: master
  # local-dir: public
